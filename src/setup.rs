

use bindings::{EntryPoint};

fn run(manager: &mut SimulationManager) -> Result<(), Box<dyn std::error::Error>> {
    // Deploy tokens

    // Setup token state, mint & approvals

    // Deploy contracts

    // Setup contract state, i.e. pools, liquidity, etc.
}