// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// For the .sol artifacts to be generated from `forge build` and thus have bindings generated
// with `forge bind`, we have to have the contracts imported into at least one .sol file
// in this local repo's contracts directory.
import "solmate/tokens/WETH.sol";
import "portfolio/Portfolio.sol";

contract IgnoreContract { }
